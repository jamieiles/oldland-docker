Oldland CPU Dockerfiles
=======================

Dockerfiles for Oldland CPU build environments:

 - jamieiles/oldland-toolchain: the GNU toolchain:
   * binutils-gdb-oldland
   * gcc-oldland
   * newlib-oldland
 - jamieiles/oldland-buildenv: includes the oldland-toolchain layer and all
 dependencies required to build the software and RTL simulations,
 oldland-debug, oldland-jtagd and the self tests.
